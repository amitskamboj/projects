# Commit Code1

Review the files that have changed, and create a commit with a commit message summrizing the changes made. Always try to give short and conscise messages that convey the business logic. Limit the number of character with the git commit command limit. Make the meaning of the commit is meaningful.

Add a pre text in the commit message like "$aegument: generated message".


